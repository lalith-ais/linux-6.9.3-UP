Linux kernel for UP board
========================

make distclean
copy initrd from buildroot/output/images/rootfs.cpio to .

make up_defconfig
make -j16 tar-pkg
./mk_kmod_squash

The efi bootable kernel is in arch/x86/boot/bzImage
copy this to <efi partition>/BOOT/EFI/BOOTX64.EFI
copy kmod squash to <SYSTEM partition>/
